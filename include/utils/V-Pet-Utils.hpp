#pragma once

#include "utils/Includes.hpp"

namespace Digivice_Size {
    // Retorna el ancho (X) de la pantalla
    unsigned int Digivice_W();

    // Retorna el alto (Y) de la pantalla
    unsigned int Digivice_H();
}